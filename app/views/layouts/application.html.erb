<!DOCTYPE html>
<html>
  <head>
    <title>Tradie websites in under 2 minutes - Get 50% OFF Today Only!</title>

    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700,800" rel="stylesheet">

  </head>

  <body>
    <% if user_signed_in? %>

        <div class="grid-x">
          <div class="small-12 menu-container">
            <ul class="menu"> 
                <li>
                  <%= link_to "View My Site", new_user_session_url(subdomain: "#{current_user.site_name}.2min.co"), class: "button warning" %></li>
                <li><%= link_to('Logout', destroy_user_session_path, method: :delete) %></li>

            </ul>
          </div>
        </div>
    <% end %>
    <%= yield %>
  </body>
</html>
