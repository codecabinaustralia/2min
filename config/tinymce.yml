default:
  menu_bar: false
  plugins:
    - link

alternate:
  menu_bar: false
  selector: textarea.table-editor
  toolbar: styleselect | bold italic | undo redo | link | alignleft aligncenter alignright | bullist numlist
  plugins:
    - link
    - lists